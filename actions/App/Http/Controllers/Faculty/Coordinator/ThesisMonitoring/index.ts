import ThesisRegistry from './ThesisRegistry'
import ProgressController from './ProgressController'
const ThesisMonitoring = {
    ThesisRegistry: Object.assign(ThesisRegistry, ThesisRegistry),
ProgressController: Object.assign(ProgressController, ProgressController),
}

export default ThesisMonitoring